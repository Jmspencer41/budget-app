spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/budgetapp
    username: ${DB_USER:budgetapp}
    password: ${DB_PASSWORD:localdev}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

server:
  port: 8080